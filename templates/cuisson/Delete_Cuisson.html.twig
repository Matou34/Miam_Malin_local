
{# ***************************************** POP UP DELETE ***************************************** #}

{% block body %}
        {# Pop Up formulaire #}
        <div class="modal fade" id="DeleteCuisson{{ cuisson.id }}"  data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="DeleteCuissonLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header popUP_Create Delete-mode">
						{# Icon Supprimer #}
						<i class="fa-solid fa-trash-can fa-3x changeColor mb-1"></i>
                        {# titre Delete #}
                        <h5 class="modal-title" id="cuissonLabel"> DELETE MODE CUISSON</h5>
                        {# Btn close #}
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
					<div class="main_delete">
						<p>Suppression du mode de cuisson
							<span class='text_color_Delete'>{{ cuisson.culibelle}}</span>
						</p>
					</div>

					<div class='SubmitRecettes'>
						<a class="m-2 btn  myBoutton affichePopUp" href={{url('Gestion_Cuissons')}}>Retour</a>
						<a class="btn myBouttonSubmit_Delete m-2" href={{url('delete_cuisson', {'id': cuisson.id} )}}>Supprimer</a>
					</div>
                </div>
            </div>
        </div>
{% endblock %}




